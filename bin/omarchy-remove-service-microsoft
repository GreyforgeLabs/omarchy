#!/bin/bash

# omarchy:summary=Remove the Outlook, Office, and Teams web apps.

set -e

for app in Outlook Office Teams; do
  if [[ -f "$HOME/.local/share/applications/$app.desktop" ]]; then
    omarchy-webapp-remove "$app"
  fi
done

echo ""
echo "Outlook, Office, and Teams have been removed."
